package com.guconstantino.watchdraw.presentation

import androidx.compose.ui.graphics.Color

// Design tokens taken from the "M3 Wear OS Apps Design Kit" prototype
// (Figma node 144999:1186).
val WatchBlack = Color(0xFF000000)
val SurfaceContainer = Color(0xFF332E3C) // side icon-button background
val SurfaceCard = Color(0xFF2B2930)      // modal/menu card background
val PrimaryOrange = Color(0xFFFF9914)    // prominent/center action
val IconOnSurface = Color(0xFFE2E2E2)    // default icon color on surfaces
val IconOnContainer = Color(0xFFE0E0E0)  // icon color on primary-container buttons
val Scrim = Color(0xCC000000)            // dimming behind modal menus

// M3 primary tonal pair (used by the stroke selector pills in the prototype).
val PrimaryContainer = Color(0xFF4D3D76) // compact-button background
val OnPrimaryLine = Color(0xFFE9DDFF)    // stroke-preview line color
